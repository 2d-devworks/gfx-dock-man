<ui:FluentWindow x:Class="GfxManWpf.ConfigurationOptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:GfxManWpf"
        mc:Ignorable="d"
        Icon="{x:Static Member=local:App.MainIconImageSource}"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        Height="300" Width="450">
    <DockPanel>
        <ui:TitleBar DockPanel.Dock="Top" Title="Gfx Configuration Option"></ui:TitleBar>
        <Grid DockPanel.Dock="Top" DataContext="{Binding Model}" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="125"></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="50"></RowDefinition>
                <RowDefinition Height="50"></RowDefinition>
                <RowDefinition Height="50"></RowDefinition>
                <RowDefinition Height="50"></RowDefinition>
                <RowDefinition Height="50"></RowDefinition>
            </Grid.RowDefinitions>
            <ui:TextBlock VerticalAlignment="Center">Name:</ui:TextBlock>
            <ui:TextBox Name="NameTextBox" Grid.Column="1" Grid.Row="0" VerticalAlignment="Center" Text="{Binding Name}"></ui:TextBox>
            <ui:TextBlock Grid.Column="0" Grid.Row="1" VerticalAlignment="Center">Display Adapter:</ui:TextBlock>
            <ui:TextBox Grid.Column="1" Grid.Row="1" VerticalAlignment="Center" Text="{Binding PrimaryDisplayDriverName}"></ui:TextBox>
            <ui:TextBlock Grid.Column="0" Grid.Row="2" VerticalAlignment="Center">Display Max Width:</ui:TextBlock>
            <ui:TextBox Grid.Column="1" Grid.Row="2" VerticalAlignment="Center" Text="{Binding PrimaryDisplayMaxWidth}"></ui:TextBox>
            <ui:TextBlock Grid.Column="0" Grid.Row="3" VerticalAlignment="Center">Display Max Height:</ui:TextBlock>
            <ui:TextBox Grid.Column="1" Grid.Row="3" VerticalAlignment="Center" Text="{Binding PrimaryDisplayMaxHeight}"></ui:TextBox>
            <ui:Button Name="SaveConfigurationButton" Grid.Column="0" Grid.Row="4" VerticalAlignment="Center" Click="SaveConfigurationButton_OnClick">Save</ui:Button>
        </Grid>
    </DockPanel>
</ui:FluentWindow>
