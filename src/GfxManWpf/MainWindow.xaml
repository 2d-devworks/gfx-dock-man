<ui:FluentWindow x:Class="GfxManWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        mc:Ignorable="d"
        Height="650" Width="800"
>
    <DockPanel>
        <ui:TitleBar DockPanel.Dock="Top" Title="{Binding TitleText, Mode=OneWay}" Icon="pack://application:,,,/GfxManWpf;component/Assets/2d-devworks.ico" ></ui:TitleBar>
        <Menu DockPanel.Dock="Top">
            <ui:MenuItem Header="_File">
                <MenuItem Header="New Game" Click="AddGameButton_OnClick"></MenuItem>
                <MenuItem Header="New Configuration" Click="AddConfigurationButton_OnClick"></MenuItem>
                <Separator />
                <MenuItem Header="E_xit" Click="OnExitClicked"></MenuItem>
            </ui:MenuItem>
        </Menu>
        <StackPanel DockPanel.Dock="Top" Margin="12" HorizontalAlignment="Left" VerticalAlignment="Center" Orientation="Horizontal" >
            <ui:TextBlock Margin="2">Active Config:</ui:TextBlock>
            <ui:TextBlock Margin="2" Text="{Binding StatusText, Mode=OneWay}" FontWeight="Bold"></ui:TextBlock>
        </StackPanel>
        <TabControl DockPanel.Dock="Top" Margin="12">
            <TabItem Header="Games">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition Height="50" />
                    </Grid.RowDefinitions>
                    <ui:ListView HorizontalAlignment="Stretch" ItemsSource="{Binding ConfiguredGames}" MouseDoubleClick="GamesListView_MouseDoubleClick">
                        <ListView.View>
                            <ui:GridView>
                                <GridViewColumn DisplayMemberBinding="{Binding Name}" Header="Name"></GridViewColumn>
                            </ui:GridView>
                        </ListView.View>
                    </ui:ListView>
                    <ui:Button Grid.Row="1" HorizontalAlignment="Left" Margin="2" Width="Auto" Height="Auto" Click="AddGameButton_OnClick">Add Game</ui:Button>
                </Grid>
            </TabItem>
            <TabItem Header="Configurations">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition Height="50" />
                    </Grid.RowDefinitions>
                    <ui:ListView HorizontalAlignment="Stretch" ItemsSource="{Binding DisplayConfigurations}" MouseDoubleClick="ConfigurationsListView_MouseDoubleClick">
                        <ListView.View>
                            <ui:GridView>
                                <GridViewColumn DisplayMemberBinding="{Binding Name}" Header="Name" Width="Auto"></GridViewColumn>
                                <GridViewColumn DisplayMemberBinding="{Binding PrimaryDisplayDriverName}" Header="Display Adapter" Width="Auto"></GridViewColumn>
                                <GridViewColumn DisplayMemberBinding="{Binding PrimaryDisplayMaxWidth}" Header="Width" Width="50"></GridViewColumn>
                                <GridViewColumn DisplayMemberBinding="{Binding PrimaryDisplayMaxHeight}" Header="Height" Width="50"></GridViewColumn>
                            </ui:GridView>
                        </ListView.View>
                    </ui:ListView>
                    <ui:Button Grid.Row="1" HorizontalAlignment="Left" Margin="2" Width="Auto" Height="Auto" Click="AddConfigurationButton_OnClick">Add Configuration</ui:Button>
                </Grid>
            </TabItem>
        </TabControl>
    </DockPanel>
</ui:FluentWindow>
