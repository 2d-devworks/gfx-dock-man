<Window x:Class="GfxManWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        mc:Ignorable="d"
        Height="450" Width="800"
        Icon="handheld.ico"
        Title="{Binding TitleText, Mode=OneWay}">
    <Grid Margin="12">
        <Grid.Resources>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>
        <StackPanel HorizontalAlignment="Left" VerticalAlignment="Center" Orientation="Horizontal" >
            <TextBlock Margin="2">Status:</TextBlock>
            <TextBlock Margin="2" Text="{Binding StatusText, Mode=OneWay}" FontWeight="Bold"></TextBlock>
        </StackPanel>
        <ListView ItemsSource="{Binding ConfiguredGames}" Grid.Row="1" Grid.Column="0" MouseDoubleClick="GamesListView_MouseDoubleClick">
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding Name}" Header="Name"></GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
        <StackPanel Grid.Row="2" HorizontalAlignment="Left" Orientation="Horizontal">
            <Button Name="AddGameButton" Margin="2" Width="75" Height="25" Click="AddGameButton_OnClick">Add Game</Button>
        </StackPanel>
    </Grid>
</Window>
